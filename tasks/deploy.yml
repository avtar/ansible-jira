---
# TODO Manage amount of memory available for JIRA

- name: Configure database connection
  template:
    src: dbconfig.xml.j2
    dest: "{{ jira_home_dir }}/dbconfig.xml"
    mode: 0644
    owner: "{{ jira_username }}"
    group: "{{ jira_groupname }}"

